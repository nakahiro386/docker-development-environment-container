version: '2.2'

services:
# common{{{
  common:
    # image: scratch
    # image: hello-world:latest
    restart: on-failure:10
    volumes:
      - &locale /etc/localtime:/etc/localtime:ro
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "10"
# }}}

# vim:set filetype=docker-compose expandtab shiftwidth=2 tabstop=2 foldmethod=marker:
